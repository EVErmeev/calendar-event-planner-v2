# Текущая задача OpenCode

## Статус

`active`

## Источник требований

Сначала открыть:

1. `docs/opencode/manifest.json`
2. документ из `active_task`
3. документ из `latest_audit`

## Текущая цель

Исправить критические и высокие замечания последнего аудита и подготовить репозиторий к повторной end-to-end валидации.

## Обязательные ограничения

- Реальные события во время разработки не создавать.
- Fixture/mock не считать подтверждением runtime-интеграции.
- Не закрывать замечания изменением только README, порога matching или интерфейса.
- Для каждого исправления добавить воспроизводящий тест.
- После выполнения обновить `manifest.json` и добавить отчёт в `validations/`.
